# Runtime dependencies for the FastAPI backend (api/) ONLY.
# Deliberately excludes streamlit and matplotlib — the API never imports them
# (verified: importing api.main pulls in neither). Keeps the Docker image lean.
# Pins mirror requirements.txt; bump in both places together.

# Core numerical stack
numpy>=2.4,<3
pandas>=3.0,<4
scipy>=1.17,<2

# Charts (Plotly figures serialised to JSON for the frontend; rasterised by
# kaleido for the PDF)
plotly>=6.8,<7
kaleido>=1.3,<2

# Market data
yfinance>=1.4,<2

# PDF report
fpdf2>=2.8,<3
Pillow>=12,<13

# API
fastapi>=0.115,<1
uvicorn>=0.34,<1
