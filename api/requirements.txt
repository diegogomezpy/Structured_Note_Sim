# API-layer deps (the FastAPI backend). The quant/runtime deps it imports live in
# the repo-root requirements.txt (numpy, pandas, plotly, yfinance, fpdf2, ...).
# Installed on top of those for the port off Streamlit.
fastapi>=0.137,<1
uvicorn[standard]>=0.49,<1
