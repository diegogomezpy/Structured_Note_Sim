// Deployment config — set this to your backend (Render) URL once, then commit.
// Example:
//   window.API_BASE = "https://structured-note-api.onrender.com";
// Leave empty for local dev (the app falls back to http://localhost:8000).
window.API_BASE = "";
