[theme]
primaryColor = "#2563eb"
backgroundColor = "#ffffff"
secondaryBackgroundColor = "#f7f9fc"
textColor = "#1a2e4a"
font = "sans serif"

[server]
# false → Streamlit opens the browser automatically on `streamlit run`.
headless = false
